import styled, { CreateStyled } from '@emotion/styled';
import { Theme } from './theme';

/**
 * Instance of emotion with the correct theme injected.
 */
export default styled as CreateStyled<Theme>;
